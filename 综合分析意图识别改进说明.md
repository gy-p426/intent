# 综合分析（Multi-Analysis）意图识别改进说明

## 改进概述

本次改进将 forecast_service 中已有的4种分析功能（周期性、环比、同比、定基比）通过统一的 `multi_analysis` 算法类型接入主项目意图识别系统，并增强了时间范围自动扩展功能。

---

## 核心变动点

### 1. 新增算法类型
- **算法标识**: `multi_analysis`
- **中文名称**: 统一多算法分析
- **支持分析**: 周期性分析、环比分析、同比分析、定基比分析

### 2. 意图识别优化
**文件**: `algorithm/router/pure_llm_selector.py`
- 添加 `multi_analysis` 算法描述（第14项）
- 增加【predict vs trend vs multi_analysis 区分规则】
- 明确触发词区分：
  - `predict`: 预测未来（触发词：预测、未来、下个月）
  - `trend`: 趋势分解（触发词：趋势分解、季节性、STL）
  - `multi_analysis`: 时间周期对比（触发词：环比、同比、与上月、定基比）

### 3. 时间范围自动扩展 ⭐ **核心功能**
**文件**: `algorithm/multi_analysis/extractor.py`
- **环比分析**: 用户说"8月环比" → 自动查询"7月+8月"数据
- **同比分析**: 用户说"8月同比" → 自动查询"去年8月+今年8月"数据
- **定基比分析**: 用户说"以2020年为基期" → 自动查询"2020年至目标年"数据
- **跨年处理**: 1月环比自动查询去年12月+今年1月

### 4. 新增 time_range_info 字段
记录时间范围扩展信息，包含：
```json
{
  "analysis_type": "period_over_period",
  "period_type": "month", 
  "current_period": {"year": 2025, "month": 8},
  "previous_period": {"year": 2025, "month": 7},
  "expanded_range": {"start": "2025-07-01", "end": "2025-08-31"}
}
```

---

## 接入状态

| 功能 | API 端点 | 接入状态 | 说明 |
|------|---------|---------|------|
| **统一多算法分析** | `/api/v1/trend/multi-analysis` | ✅ 已接入 | 一次调用支持多种分析 |
| 周期性分析 | `/api/v1/trend/periodicity` | ⚠️ 间接支持 | 通过 multi-analysis 调用 |
| 环比分析 | `/api/v1/trend/period-over-period` | ⚠️ 间接支持 | 通过 multi-analysis 调用 |
| 同比分析 | `/api/v1/trend/year-over-year` | ⚠️ 间接支持 | 通过 multi-analysis 调用 |
| 定基比分析 | `/api/v1/trend/base-period-index` | ⚠️ 间接支持 | 通过 multi-analysis 调用 |

---

## 配置更新

### algorithm_input.json
```json
{
  "multi_analysis": {
    "name": "统一多算法分析",
    "description": "支持周期性、环比、同比、定基比分析",
    "required_fields": ["timestamp_column", "value_column"],
    "optional_fields": ["analysis_types", "period_type", "time_range_info"],
    "sub_algorithms": {
      "periodicity": "周期性分析",
      "period_over_period": "环比分析", 
      "year_over_year": "同比分析",
      "base_period_index": "定基比分析"
    }
  }
}
```

### intent.txt
新增 33 条 `multi_analysis` 相关意图示例：
- 周期性：分析销售数据的周期性规律
- 环比：分析本月销售额与上月的环比变化
- 同比：分析今年销售额与去年同期的对比
- 定基比：以2020年为基期计算各年度销售指数

---

## 技术架构

```
用户提问 → 意图识别(multi_analysis) → 参数提取(时间范围扩展) → SQL生成 → forecast_service调用
```

**关键特点**:
- **远程服务**: 复用现有 forecast_service（端口8100）
- **统一格式**: 返回 `{"解释": "...", "算法结果": {...}}` 格式
- **服务发现**: 支持 Nacos 服务发现 + 静态URL降级

---

## 测试覆盖

| 测试文件 | 测试数量 | 覆盖范围 |
|---------|---------|---------|
| `test_multi_analysis_time_range.py` | 43+ 个属性测试 | 时间范围解析、验证逻辑 |
| `test_multi_analysis_intent_flow.py` | 8 个流程测试 | 意图识别、边界情况 |
| `verify_checkpoint.py` | 7 个检查点 | 端到端验证 |

**测试状态**: ✅ 全部通过

---

## 用户体验改进

### 改进前
```
用户: "分析8月销售额的环比变化"
系统: 只查询8月数据 → 无法计算环比 → 分析失败
```

### 改进后  
```
用户: "分析8月销售额的环比变化"
系统: 自动扩展查询7月+8月数据 → 成功计算环比 → 返回分析结果
```

---

## 风险评估

| 风险点 | 影响程度 | 缓解措施 |
|--------|----------|----------|
| LLM时间理解偏差 | 中 | 详细提示词规则 + 测试覆盖 |
| forecast_service依赖 | 高 | 服务发现 + 静态URL降级 |
| 字段名匹配问题 | 中 | 与其他算法一致的处理方式 |

---

## 符合需求确认

✅ **功能完整性**: 支持所有4种分析类型  
✅ **用户体验**: 时间范围自动扩展，减少用户认知负担  
✅ **技术架构**: 复用现有服务，无额外部署成本  
✅ **测试覆盖**: 43+个属性测试，保证质量  
✅ **向后兼容**: 不影响现有算法功能  

**建议**: 可投入生产使用，建议监控 forecast_service 服务可用性。

---

*生成时间: 2025-01-17*  
*版本: v1.0*