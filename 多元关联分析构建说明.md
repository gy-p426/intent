# 多元关联分析构建说明

### 1.输入格式--JSON

```json
{
  "data": [
    { "name": "column_A", "values": [] },
    { "name": "column_B", "values": [] },
    { "name": "column_C", "values": [] },
    { "name": "column_D", "values": [] }
  ],
  "options": {
    "analysis_mode": "bivariate | pairwise | multivariate"
  }
}
```

输入数据说明：

1. data：列名+对应数据，数组类型，支持多变量
2. analysis_mode：分析模式：包含bivariate（二元关联分析），pairwise（多变量两两关联分析），multivariate（多变量综合关联分析，此时应将因变量设置为列1）



### 2.算法实现步骤-python（intent->config,extractor,processor    algorithm->association-service）

1. 数据提取（大模型，RAG）
2. 数据验证（数据缺失，异常，bivariate模式时是否只有两列数据，其他模式是否是三列及以上数据->大模型生成）
3. 数据类型检测：应该建立一个数组用来保存每一列的数据类型值（连续，离散），方便根据需要进行后续处理
4. bivariate（二元关联分析）:基于之前的三种方法（卡方，相关性分析，方差）
5. pairwise（多变量两两关联分析）:互信息算法（分箱/KNN），热力图？矩阵？
6. multivariate（多变量综合关联分析，此时应将因变量设置为第一列）：CMI+回归



### 3.算法输出结果：

#### 1.输出格式--json

```json
{
  "解释": "……",
  "算法结果": {
    "方法": "",
    "使用模式": "",
    "结论摘要": {
        "存在依赖的变量对": [],
 		"不存在显著依赖的变量对": []
    },
    "显著性": {
        "判定方式": "",
  		"阈值": ""
    },
    "算法细节": {}
  }
}

```

输出格式参数说明：

1. 解释：自然语言简要说明结论

2. 算法结果：

   - 方法：采取什么方法（卡方，相关性分析，方差，MI，CMI+回归 。。。）

   - 使用模式（bivariate | pairwise | multivariate）

   - 结论摘要，显著性

   - 算法细节：不同算法独特的输出

     

#### 2.输出格式--图表

​		pairwise：互信息矩阵热力图